<Window x:Class="BlackBox.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:BlackBox"
        mc:Ignorable="d"
        Title="BlackBox"
        Background="{StaticResource BgBrush}"
        Height="800"
        Width="900"
        MinHeight="600"
        MinWidth="700"
        WindowStartupLocation="CenterScreen">

    <Grid Margin="40,30,40,40">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" HorizontalAlignment="Center">
            <TextBlock Text="BlackBox"
                       FontFamily="Segoe UI"
                       FontSize="40"
                       FontWeight="Bold"
                       Foreground="{StaticResource AccentBrush}"
                       HorizontalAlignment="Center"/>
            <TextBlock Text="Save and restore file snapshots"
                       FontFamily="Segoe UI"
                       FontSize="14"
                       Foreground="{StaticResource MutedTextBrush}"
                       HorizontalAlignment="Center"
                       Margin="0,2,0,0"/>
        </StackPanel>

        <Border Grid.Row="1"
                Background="{StaticResource CardBrush}"
                BorderBrush="{StaticResource CardBorderBrush}"
                BorderThickness="1"
                CornerRadius="20"
                Padding="20,8"
                HorizontalAlignment="Center"
                Margin="0,24,0,0">
            <TextBlock x:Name="FileCountDisplay"
                       Text="File Count: 0"
                       FontFamily="Segoe UI"
                       FontSize="18"
                       Foreground="{StaticResource TextBrush}"/>
        </Border>

        <StackPanel Grid.Row="2"
                    Orientation="Horizontal"
                    HorizontalAlignment="Center"
                    Margin="0,28,0,28">
            <Button Content="Save Snapshot"
                    Style="{StaticResource AccentButton}"
                    Width="220"
                    Margin="0,0,16,0"
                    Click="SelectSnapshot"/>
            <Button Content="Resurrect Snapshot"
                    Style="{StaticResource GhostButton}"
                    Width="220"
                    Click="OpenRestoreWindow"/>
        </StackPanel>

        <Border Grid.Row="3"
                MaxWidth="700"
                Background="{StaticResource CardBrush}"
                BorderBrush="{StaticResource CardBorderBrush}"
                BorderThickness="1"
                CornerRadius="12"
                Padding="6">
            <TextBox x:Name="SnapshotDisplay"
                     IsReadOnly="True"
                     TextWrapping="Wrap"
                     AcceptsReturn="True"
                     Background="Transparent"
                     Foreground="{StaticResource TextBrush}"
                     CaretBrush="{StaticResource AccentBrush}"
                     SelectionBrush="{StaticResource AccentPressedBrush}"
                     FontFamily="Consolas"
                     FontSize="14"
                     BorderThickness="0"
                     Padding="12"
                     VerticalScrollBarVisibility="Auto"
                     HorizontalScrollBarVisibility="Auto"/>
        </Border>
    </Grid>
</Window>